apiVersion: v1
kind: Service
metadata:
  name: {{ include "status-api.fullname" . }}
  labels:
    {{- include "status-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "status-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
